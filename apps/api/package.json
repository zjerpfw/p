{
  "name": "@crm/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "check": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "clean": "wrangler delete --dry-run"
  },
  "dependencies": {
    "@crm/db": "workspace:*",
    "aws4fetch": "^1.0.20",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.8.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250816.0",
    "typescript": "^5.8.3",
    "wrangler": "4.31.0"
  }
}
